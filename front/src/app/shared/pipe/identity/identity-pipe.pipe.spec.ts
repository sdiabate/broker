import { IdentityPipePipe } from './identity-pipe.pipe';

describe('IdentityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IdentityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
