import { FlexLayoutGapDirective } from './flex-layout-gap.directive';

describe('FileIconDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLayoutGapDirective();
    expect(directive).toBeTruthy();
  });
});
